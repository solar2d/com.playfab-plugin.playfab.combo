Sources for the plugin `plugin.playfab.combo`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.playfab.combo" = {
            publisherId = "com.playfab",
        },
    },
}
```
